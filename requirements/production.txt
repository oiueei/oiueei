-r base.txt

# Exact pins, for the reason written at the top of base.txt: this is the file
# `pip-audit` runs against in CI *and* the one Heroku installs, so they must not
# be able to resolve differently.

# Database
psycopg2-binary==2.9.12
dj-database-url==3.1.2

# Production server
gunicorn==26.0.0
whitenoise==6.12.0
